# Program Flow

## Error Messages

Note: Şimdilik get_next_line hatalarını es geçiyorum.

1. Input-Map Errors -> "Error\n`message`"

2. `open()`, `malloc()` Errors -> `perror("Error")`

## Check Input

1. Check input number with main argument.
   -> "Error\nInput number wrong!"
2. Check invalid lines.

- creates a file descriptor
- creates lines with dynamic memory

3. Check identifiers.

- creates a file descriptor
- creates lines with dynamic memory
- uses ft_split

-> Checks existence and uniqueness

4. Check color format.

- creates a file descriptor
- creates lines with dynamic memory
- uses ft_split twice

Starting map check

5. Ma
